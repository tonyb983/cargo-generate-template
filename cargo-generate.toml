[template]
cargo_generate_version = ">=0.10.0"
ignore = [ "README.md", ".gitignore", ".vscode" ]
# include = [ "..." ]
# exclude = [ "..." ]

[hooks]
pre = [ "pre-setup.rhai" ]
post = [ "post-setup.rhai" ]
