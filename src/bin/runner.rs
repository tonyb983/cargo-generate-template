//! `runner` bin to use during library development

fn main() {
    println!("{{project-name}} Runner");
    println!();
    println!("Use this bin if you need to run something during development!");
    println!("It is setup as the default entrypoint if you execute `cargo run` with no additional arguments.");
}